---
title: "Store API Keys and Environment Variables in Rork"
sidebarTitle: "Secrets"
description: "Keep API keys out of your code with Rork Secrets. Understand the server-side vs client-side split and how to rotate a key that was exposed."
---

Most useful apps need a key: a weather API, an email sender, a payment provider. Those keys belong in your project's **Secrets**, not in your code and not in a chat message. Open **More → Secrets** in a project.

<Steps>
  <Step title="Press Add Variable">
    A new row appears with a **Key** and a **Value**.
  </Step>
  <Step title="Type the name in capitals with underscores">
    Use the exact name the provider's documentation uses, for example `RESEND_API_KEY`, `STRIPE_SECRET_KEY`, or `OPENWEATHER_API_KEY`.
  </Step>
  <Step title="Paste the value">
    Rork hides it straight away. The eye button reveals it when you need to check it.
  </Step>
  <Step title="Press Save">
    Rork stores the variable and rebuilds the parts of your app that need it.
  </Step>
</Steps>

Adding several at once is faster with **Bulk Edit**. Paste one variable per line as `KEY=value`.

## How far a variable travels

The label under each key tells you who can read that value:

| Label | Where the value goes |
| --- | --- |
| **Server-side only** | Stays on the server. Your users can never read it. |
| **Available at build time** | Baked into the app when it is built. |
| **Available to all users** | Shipped inside the app, readable by anyone who installs it. |

The rule is the name. A variable whose name starts with `EXPO_PUBLIC_` is sent to the app itself, and everything else stays server-side.

<Warning>
  Never give a secret key an `EXPO_PUBLIC_` name, and never paste one into your app's code. Anything shipped inside an app can be extracted from it. A private key belongs in a **Server-side only** variable, used by a server function.
</Warning>

Two examples make the split clear:

- `EXPO_PUBLIC_SUPABASE_URL` is fine as a public value. It is an address, protected by database rules.
- `STRIPE_SECRET_KEY` must be **Server-side only**. It can move money.

If your feature needs a private key, ask Rork for a server function:

```text
Send the confirmation email from a server function so the Resend key stays private
```

## When Rork asks you for a key

You usually do not open the Secrets editor at all. When a build needs a key, Rork stops and asks in chat, with one field per key and a short description of what each one is for. Paste the values and press **Save and Continue**.

## Who can see your keys

| Role | Secrets |
| --- | --- |
| Owner | Can read and edit |
| Editor | Can read and edit |
| Viewer | Sees the panel, cannot edit |

An editor on your project can read your keys. Invite editors accordingly.

## Rotate a key you leaked

If a key ends up somewhere public, changing it in Rork is not enough. Do it in this order:

<Steps>
  <Step title="Revoke the key at the provider">
    Delete or rotate it in the provider's own dashboard. That is what actually stops the key from working.
  </Step>
  <Step title="Create a new key">
    Copy the new value.
  </Step>
  <Step title="Update the variable in Rork">
    Same key name, new value, then **Save**.
  </Step>
  <Step title="Check where it was exposed">
    If it was in your code, ask Rork to remove it and to read the value from the variable instead.
  </Step>
</Steps>

## FAQ

<AccordionGroup>
  <Accordion title="Should I paste my API key into the chat?">
    Prefer the Secrets editor, or the card Rork shows when it needs a key. Both keep the value out of the conversation.
  </Accordion>

  <Accordion title="Do secrets go to GitHub?">
    No. They live with the project, not in your code, so they are not committed. Set them again in any environment you run the code yourself.
  </Accordion>

  <Accordion title="Are secrets per project or per account?">
    Per project. A project with an iPhone app and a web app shares one set of variables.
  </Accordion>

  <Accordion title="I added a variable and the app still cannot see it.">
    Reload the preview first. If it is a native app, the value is baked in at build time, so it needs a new build. If the name is missing the `EXPO_PUBLIC_` prefix, the app cannot read it by design.
  </Accordion>

  <Accordion title="Do I need my own OpenAI key to use AI in my app?">
    Not necessarily. Rork Cloud gives your app models without your own provider keys. See [What is Rork Cloud](/backend/rork-cloud).
  </Accordion>

  <Accordion title="Where do Supabase keys go?">
    Connecting Supabase sets them for you and marks them as managed. See [How to add a Supabase backend](/backend/supabase).
  </Accordion>
</AccordionGroup>

## Next steps

<CardGroup>
  <Card title="Connect an API" icon="plug" href="/features-apis/apis/how-to-connect-an-api-to-your-project">
    Use a third-party API with your new key.
  </Card>

  <Card title="Rork Cloud" icon="database" href="/backend/rork-cloud">
    A backend inside Rork, with server-side keys handled for you.
  </Card>

  <Card title="Share the Project" icon="users" href="/collaborate/share-a-project">
    Who can read your secrets, and who cannot.
  </Card>
</CardGroup>
